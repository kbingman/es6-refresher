var main = function () {
  var name = 'Tatooine'; 
  var name = 'Dantooine';
  console.log(name);
};

main();

// Exercise 1
// ================
// Update the function itself to use `const` instead of `var`

var main = function () {
  var name = 'Tatooine'; 
  var name = 'Dantooine';
  console.log(name);
};

main();

// Exercise 2
// ================
// Update the variables  to use `let` instead of `var`, but keep the reassignement

const planet = {
  name: 'Alderaan',
  inhabitants: 1000000000
};

// Exercise 3 
// ================
// Add attributes to an existing object then reassign them

const planets = [];
// Exercise 4 
// ================
// Add items to an existing array

